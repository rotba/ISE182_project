<Window x:Class="ChatRoom_project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRoom_project"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Button Content="Add Message" HorizontalAlignment="Left" Height="47" Margin="362,262,0,0" VerticalAlignment="Top" Width="145" Click="Button_Click"/>
        <Button Content="Sort1" HorizontalAlignment="Left" Margin="217,262,0,0" VerticalAlignment="Top" Width="140" Height="47" Click="Button_Click_1"/>
        <ListBox Name="lbMessages" Margin="10,10,10,62">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding UserName}" FontWeight="Bold" />
                        <TextBlock Text=": " FontWeight="Bold" />
                        <TextBlock Text="{Binding MessageContent}" Width="225" TextWrapping="Wrap" HorizontalAlignment="Stretch" />
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Sort2" HorizontalAlignment="Left" Margin="94,262,0,0" VerticalAlignment="Top" Width="118" Height="47" Click="Button_Click_2"/>
    </Grid>
</Window>
