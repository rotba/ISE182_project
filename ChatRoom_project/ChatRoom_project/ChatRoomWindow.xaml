<Window x:Class="ChatRoom_project.ChatRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRoom_project"
        mc:Ignorable="d"
        Title="ChatRoomWindow" Height="350" Width="525">
    <Grid>
        <Button Content="Filter" HorizontalAlignment="Left" Height="47" Margin="362,195,0,0" VerticalAlignment="Top" Width="145" Click="Button_Click"/>
        <ListBox Name="lbMessages" Margin="10,10,161.4,63.8">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Text="{Binding UserName}" FontWeight="Bold" />
                        <TextBlock Text=": " FontWeight="Bold" />
                        <TextBlock Text="{Binding MessageContent}" Width="225" TextWrapping="Wrap" HorizontalAlignment="Stretch" />
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Sort" HorizontalAlignment="Left" Margin="362,143,0,0" VerticalAlignment="Top" Width="145" Height="47" Click="Button_Click_2"/>
        <Button x:Name="logout" Content="logout" HorizontalAlignment="Left" Margin="445,260,0,0" VerticalAlignment="Top" Width="62" Click="logout_Click" Height="49"/>
        <Button Content="Send" HorizontalAlignment="Left" Margin="10,260,0,0" VerticalAlignment="Top" Width="76" Click="Button_Click_3" Height="49"/>
        <TextBox HorizontalAlignment="Left" Height="49" Margin="91,260,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="265"/>
        <GroupBox Header="Sort by:" HorizontalAlignment="Left" Height="99" Margin="362,10,0,0" VerticalAlignment="Top" Width="145">
            <CheckBox Content="CheckBox" HorizontalAlignment="Left" Margin="40,33,0,0" VerticalAlignment="Top"/>
        </GroupBox>
    </Grid>
</Window>
